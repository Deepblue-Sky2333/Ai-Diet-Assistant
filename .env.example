# ============================================
# AI Diet Assistant Environment Configuration
# ============================================
# 
# 重要提示：此文件仅作为示例，不要直接使用！
# 运行 'scripts/install.sh' 会自动生成安全密钥并创建 .env 文件
#
# IMPORTANT: This is an example file. DO NOT use these values in production!
# Run 'scripts/install.sh' to automatically generate secure keys and create your .env file.
#

# ============================================
# Server Configuration
# ============================================
# 由安装脚本配置 / Configured by install script
SERVER_PORT=9090
SERVER_MODE=release  # Options: debug, release, test

# ============================================
# Database Configuration [REQUIRED]
# ============================================
# 由安装脚本配置 / Configured by install script
DB_HOST=localhost
DB_PORT=3306
DB_USER=diet_user
DB_PASSWORD=<AUTO_CONFIGURED_BY_INSTALL_SCRIPT>
DB_NAME=ai_diet_assistant

# ============================================
# JWT Configuration [REQUIRED]
# ============================================
# 由安装脚本自动生成 / Automatically generated by install script
JWT_SECRET=<AUTO_GENERATED_48_BYTES_BASE64>

# ============================================
# Encryption Configuration [REQUIRED]
# ============================================
# 由安装脚本自动生成 / Automatically generated by install script
ENCRYPTION_KEY=<AUTO_GENERATED_32_BYTES>

# ============================================
# Redis Configuration [OPTIONAL]
# ============================================
# 由安装脚本配置 / Configured by install script
# Redis 用于令牌黑名单管理（登出功能）
# 如果禁用，系统将使用内存存储（不推荐用于生产环境）
#
# Redis is used for token blacklist management (logout functionality)
# If disabled, the system will use in-memory storage (not recommended for production)
#
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# Rate Limiting [OPTIONAL]
# ============================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# ============================================
# CORS Configuration [REQUIRED]
# ============================================
# 由安装脚本配置 / Configured by install script
# ⚠️  安全警告：CORS 对于防止未授权访问至关重要！
# ⚠️  SECURITY WARNING: CORS is critical for preventing unauthorized access!
#
# 重要规则 / IMPORTANT RULES:
# - ❌ 禁止使用通配符 (*) / NEVER use wildcard (*)
# - ✅ 使用完整 URL / Use complete URLs: http://example.com
# - ✅ 多个域名用逗号分隔 / Separate multiple domains with commas
#
# 注意：HTTPS 由 Nginx 反向代理处理
# Note: HTTPS is handled by Nginx reverse proxy
#
CORS_ALLOWED_ORIGINS=<AUTO_CONFIGURED_BY_INSTALL_SCRIPT>

# ============================================
# Logging [OPTIONAL]
# ============================================
LOG_LEVEL=info  # Options: debug, info, warn, error
LOG_FORMAT=json  # Options: json, console

# ============================================
# Security Settings [OPTIONAL]
# ============================================
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=15m
PASSWORD_MIN_LENGTH=8
