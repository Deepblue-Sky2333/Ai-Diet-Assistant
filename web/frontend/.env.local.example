# AI Diet Assistant Frontend Environment Variables
# Copy this file to .env.local and update the values

# ============================================
# Demo Mode Configuration
# ============================================
# Set to 'true' to run the app without a backend (uses mock data)
# Set to 'false' to connect to a real backend API
NEXT_PUBLIC_DEMO_MODE=true

# ============================================
# Backend API Configuration
# ============================================
# Only required when NEXT_PUBLIC_DEMO_MODE=false

# Local development (backend running on port 9090)
NEXT_PUBLIC_API_URL=http://localhost:9090/api/v1

# Production deployment (replace with your actual backend URL)
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api/v1

# ============================================
# Usage Instructions
# ============================================
# 
# For v0 Users:
# - Add these variables in the "Vars" section (left sidebar)
# - Do NOT create .env.local file in v0
#
# For Local Development:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Update the values as needed
# 3. Restart your dev server: npm run dev
#
# Demo Mode (Recommended for Testing):
# - No backend setup required
# - Use test account: username=test, password=114514
# - All features work with sample data
#
# Production Mode:
# - Requires backend API running
# - Set NEXT_PUBLIC_DEMO_MODE=false
# - Configure NEXT_PUBLIC_API_URL to your backend
